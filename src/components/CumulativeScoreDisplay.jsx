import { useUser } from "../context/UserContext";
import { useLocation } from "react-router-dom";

/**
 * Cumulative Score Display Component
 * 
 * DISABLED - Score summary shown on /complete page instead
 * This component intentionally returns null everywhere
 */
export default function CumulativeScoreDisplay() {
  // Disabled - final score shown on /complete page after game ends
  return null;
}
